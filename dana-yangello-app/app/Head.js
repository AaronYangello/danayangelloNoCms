export default function Head({pageName}){
    return (
        <head>
            <link rel="icon" href='/img/favicon.ico'/>
            <title>{`Dana Yangello | ${pageName}`}</title>
        </head>
    );
}